Lankaora V41 — Navigation + Journey Builder Recovery

Fixes:
- Shared visible desktop/mobile navigation on every HTML page, including nested destination and region pages.
- Light-page navigation now has readable dark text; home keeps transparent white navigation over the hero.
- Requested image-less pages use solid editorial colour rather than broken/empty image space.
- Build My Trip restored as a real planner: focus, where/region, days, pace and planning budget.
- Planner produces a starting route and clearly labels budget as a planning range, not a live quote.
- Plan Your Trip remains the inquiry/conversion destination; no payment gateway added.

Next update proposal (V42):
- Consolidate the planner state so selections persist between pages.
- Improve destination/region cross-links and breadcrumbs.
- Add structured FAQ content to the highest-value destination pages.
- Add Search Console-ready technical checks: sitemap coverage, canonical consistency, broken-link scan, page titles/meta audit.
- Polish mobile navigation and accessibility (focus states, keyboard/aria behavior).
